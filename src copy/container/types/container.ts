export type T_BasicContainerProps = {
    className?: string;
    children: React.ReactNode;
};

// TODO: Need to think if we even need anything else than BasicContainer, which then is Container
export type T_ContainerProps = T_BasicContainerProps;
